# secrets
.env.*
.gitconfig

# histfiles
.irb_history
.history

# danbooru local config
/config/danbooru_local_config.rb

# tmp folders
/log/
/tmp/

# javascript cache folders
/node_modules/
/public/data/
/public/packs-dev/
/public/packs-test/
/public/packs/

# dtext compilation artifacts
lib/dtext_rb/ext/dtext/Makefile
lib/dtext_rb/ext/dtext/dtext.cpp

# devcontainer features lock
.devcontainer/devcontainer-lock.json
/.testimg/

# Guard: brain/ops docs and deployment config belong in fourier-basis
# (private), never in this public repo. CLAUDE.md was tracked here until
# 2026-08-25; the deployment's local config, its .env and the fork devlog
# until 2026-09-11. Canon for the docs is fourier-basis docs/claude and
# docs/repos/chanbooru (hydrated in); the two runtime files live on the
# serving box and in fourier-basis ops, observed by coherence filedrift.
#
# NOTE: untracking stops the NEXT commit, not the last one. The content
# remains in this repo's history and on GitHub's. Nothing in it was a
# credential, so rotation is not the remedy.
CLAUDE.md
FORK_DEVLOG.md
config/danbooru_local_config.rb
.env